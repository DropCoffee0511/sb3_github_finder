# 🌌 GitHub Finder

> **깃허브의 전 세계 사용자 프로필과 인기 오픈소스 저장소를 실시간으로 스마트하게 검색하고 탐색하는 반응형 웹 애플리케이션입니다.**

본 프로젝트는 **Vanilla HTML, CSS, JavaScript**만을 사용하여 구현되었으며, 깃허브 API를 연동하여 세련되고 모던한 UI(글래스모피즘, 네온 그라데이션)로 데이터를 보여줍니다.

---

## ✨ 주요 기능 (Key Features)

1. **👤 스마트 개발자 검색 (User Profiling)**
   - 검색어 앞에 `@` 기호를 붙여 검색합니다. (예: `@DropCoffee0511`)
   - 해당 사용자의 아바타 이미지, 닉네임, 소개글(Bio), 팔로워 및 팔로잉 수, 공개 저장소 개수를 가로 와이드 카드로 보여줍니다.
   - 해당 개발자의 공개 저장소 중 **스타(Star) 개수가 가장 높은 인기 저장소 6개**를 정렬하여 보여줍니다.

2. **🏷️ 기술 주제 검색 (Topic Search)**
   - `@` 기호 없이 기술명이나 키워드를 검색합니다. (예: `react`, `python`)
   - 해당 키워드를 주제로 하는 전 세계 저장소 중 **스타(Star) 개수가 가장 높은 인기 저장소 12개**를 3열 4행 박스 그리드로 가득 채워 보여줍니다.
   - 각 저장소 카드 내부에는 깃허브 토픽 분류 태그(`#react`, `#frontend` 등)가 함께 출력되어 직관적입니다.

3. **💡 추천 키워드 및 원클릭 자동 검색**
   - 첫 화면의 가이드 카드에 제공되는 추천 배지를 클릭하면 자동으로 검색창에 입력되고 즉시 검색이 수행되어 사용자 편의성을 높였습니다.

4. **📱 모던 & 반응형 디자인**
   - 데스크톱뿐만 아니라 태블릿, 모바일 기기에서도 화면 크기에 맞추어 레이아웃이 자연스럽게 변경되는 그리드 디자인 시스템을 적용했습니다.

---

## 🛠️ 기술 스택 (Tech Stack)

- **Markup**: `HTML5` (시맨틱 구조화)
- **Styling**: `Vanilla CSS3` (CSS 변수 활용 테마, 밤하늘 네온 애니메이션, Flexbox/Grid)
- **Logic**: `Vanilla JavaScript (ES6)` (클래스 기반 API 및 UI 컴포넌트 관리, Async/Await 비동기 통신)
- **API**: `GitHub REST API`
- **Icon**: `FontAwesome 6`

---

## 📝 1일차 개발 일지: 피드백 반영 및 점진적 개선 (5/23)

어제(5/23) 하루 동안 관리자님의 피드백을 통해 뼈대 구성부터 디테일한 레이아웃 개선까지 점진적으로 진행된 대화 내용입니다.

### 📌 1단계: 최초 기획 및 설계 (Prompt #1)
*   **User**: 자바스크립트를 이용하여 github finder 앱을 만들고 싶습니다. 필수 화면(검색창, 프로필, 저장소 목록, 상태메시지)을 포함하고, 프로필 매핑 정보(이름, 사진, 소개글), 통계(팔로워, 팔로잉, 공개 저장소 수), 저장소 목록(이름 및 외부 링크)을 연동하며 헤더 제목은 "Github Finder"로 지정해 주세요.
*   **Gemini**: 최고급 UI 레이아웃과 깃허브 API를 연동하여 1차 기초 구조를 설계하고 구현 계획서를 작성했습니다.

### 📌 2단계: 자동 로드 및 스타 순 정렬 추가 (Prompt #2)
*   **User**: 처음 사이트에 접속했을 때 깃허브 공식 계정(`github`)의 프로필을 기본으로 보여주고, 저장소 목록은 스타(Star) 개수가 높은 순서로 정렬해 주세요.
*   **Gemini**: DOM이 로드될 때 자동으로 `github` 프로필과 리포지토리를 스타 순으로 병렬 요청하여 그리드에 바인딩하는 로직을 추가했습니다.

### 📌 3단계: 1차 UI/UX 개선 피드백 (Prompt #3)
*   **User**: 
    1. 배경이 무조건 어두운 블랙인 것은 선호하지 않습니다. 시스템/브라우저 기본 설정을 따르도록 수정해 주세요.
    2. 검색창 세로 폭이 너무 넓어 답답합니다. 좀 더 슬림하고 컴팩트하게 다듬어 주세요.
    3. 첫 화면에 특정 프로필이 바로 나오는 것보다, 검색창만 나오고 그 밑에 검색 방법을 친절하게 설명하는 가이드가 나왔으면 좋겠습니다.
*   **Gemini**: 
    - 사용자의 OS/브라우저 테마를 감지하는 미디어 쿼리를 연동하여 다크/라이트 자동 전환 테마를 적용했습니다.
    - 검색창 세로 폭을 조절하여 컴팩트하게 재조정했습니다.
    - 첫 접속 시 프로필을 숨기고 검색 대상(사용자 검색은 `@`, 주제 검색은 단어 입력)을 설명하는 미려한 안내 가이드 카드를 전면에 배치했습니다.

### 📌 4단계: 기술 주제(Topic) 검색 도입 및 3열 레이아웃 (Prompt #4)
*   **User**: 
    1. 지금은 사용자만 찾을 수 있는데, 오픈소스 기술명(주제)도 검색하여 전 세계의 관련 저장소를 모아볼 수 있게 해주세요.
    2. 검색창 폭을 조금 더 작게 만들어 주세요.
    3. 저장소 목록 카드는 맨 하단에 3열 가로 그리드 박스 배열로 배치해 주세요.
*   **Gemini**: 
    - 깃허브 토픽 검색 API(`https://api.github.com/search/repositories?q=topic:기술명`)를 연동하여 주제 검색 기능을 구현했습니다.
    - 디자인 가이드라인에 맞춘 초소형 미니멀 검색창을 적용했습니다.
    - 하단 저장소 목록을 3열 구조의 입체 카드 레이아웃으로 변경했습니다.

### 📌 5단계: 검색창 단일화 및 프로필 가로형 개선 (Prompt #5)
*   **User**: 
    1. 사용자 검색과 주제 검색을 별도 버튼이나 토글로 나누지 말고, 입력값 앞에 `@` 기호가 붙으면 사용자 검색, 없으면 주제 검색으로 시스템이 똑똑하게 판단하게 해주세요.
    2. 사용자 검색 결과의 경우, 검색창 바로 밑에 프로필 정보를 먼저 띄우고 그 아래에 사용자의 리포지토리를 스타 순으로 노출해 주세요.
    3. 검색창 입력 박스의 세로 폭을 한층 더 줄여 슬림하게 디자인하고, 돋보기 버튼을 입력창 우측 내부에 자연스럽게 녹아들게 해주세요.
    4. 프로필 정보 카드가 세로로 너무 깁니다. 왼쪽에는 아바타 이미지를 배치하고 우측에 이름, 설명, 통계치(팔로워 등)와 방문 버튼이 오는 와이드 가로형 카드로 변경해 주세요.
*   **Gemini**: 
    - 접두사 `@` 기반의 스마트 라우팅 검색 컨트롤러를 완성했습니다.
    - 검색 박스 내부에 검색 버튼을 돋보기 아이콘으로 통합하여 디자인 완성도를 높였습니다.
    - 프로필 카드를 가로형 와이드 슬림 레이아웃으로 재구성하여 가독성을 극대화했습니다.

---

## 📝 2일차 개발 일지: 로컬 저장소 격리 및 깃허브 업로드 (5/24)

오늘(5/24)은 로컬 환경의 비정상적인 깃 설정을 해결하고, 안전하게 깃허브 저장소를 생성하여 소스 코드를 최종 업로드 및 배포 완료했습니다.

### 📌 1단계: 사용자 정보 및 깃허브 저장소 확인
*   **User**: 깃허브에 올릴 사용자 이름은 `DropCoffee0511`, 이메일은 `sonsc076@gmail.com`, 저장소 주소는 `https://github.com/DropCoffee0511/sb3_github_finder`입니다.
*   **Gemini**: 사용자 정보와 주소를 접수하여 업로드 준비를 시작했습니다.

### 📌 2단계: 로컬 깃 저장소 새로 만들기
- **이유**: 기존에 상위 홈 디렉터리(`C:/Users/chengziacc2`)에 깃이 잘못 설정되어 있어, 이 프로젝트만을 관리하기 위한 전용 저장소로 분리/초기화했습니다.
- **명령어**:
  ```bash
  git init
  ```
- **결과**: `C:/Users/chengziacc2/projects/sb3_githubfinder/.git/` 하위에 독립된 빈 깃 저장소가 정상 생성되었습니다.

### 📌 3단계: 로컬 깃 작성자 설정 등록
- **이유**: 커밋 이력에 올바른 개발자명과 이메일이 기록되도록 로컬 설정을 지정했습니다.
- **명령어**:
  ```bash
  git config user.name "DropCoffee0511"
  git config user.email "sonsc076@gmail.com"
  ```
- **결과**: 작성자 정보 등록이 성공적으로 완료되었습니다.

### 📌 4단계: 최초 커밋 및 브랜치명 변경
- **이유**: `.gitignore`, `app.js`, `index.html`, `style.css` 소스 파일을 깃에 보존하고, 기본 브랜치명을 표준 규격인 `main`으로 변경했습니다.
- **명령어**:
  ```bash
  git add .
  git commit -m "Initial commit"
  git branch -M main
  ```
- **결과**: 로컬 저장소에 첫 번째 전송용 `main` 브랜치 커밋이 기록되었습니다.

### 📌 5단계: 원격 연결 및 최종 업로드 (Push)
- **이유**: 깃허브 원격 주소를 등록하여 모든 소스 코드 파일을 깃허브 저장소로 밀어올립니다.
- **명령어**:
  ```bash
  git remote add origin https://github.com/DropCoffee0511/sb3_github_finder
  git push -u origin main
  ```
- **결과**: 깃허브 원격 저장소 업로드가 안전하게 완료되었으며 브랜치 추적이 시작되었습니다.

---

## 🚀 프로젝트 실행 방법 (How to Run)

1. 이 저장소를 로컬 컴퓨터로 복제(Clone)합니다.
   ```bash
   git clone https://github.com/DropCoffee0511/sb3_github_finder.git
   ```
2. 프로젝트 폴더로 이동합니다.
   ```bash
   cd sb3_github_finder
   ```
3. 브라우저에서 `index.html` 파일을 직접 실행하거나, VS Code의 Live Server 확장을 사용하여 개발 서버를 실행해 접속합니다.

### 🐳 Docker를 이용한 웹 서버 구동 (Production Deployment)

관리자님의 Proxmox 서버나 Docker가 설치된 환경에서 웹 서버로 상시 서비스하고 싶으시다면, 제공되는 `Dockerfile`과 `docker-compose.yml`을 사용하여 아주 간단하게 구동할 수 있습니다.

1. **컨테이너 빌드 및 백그라운드 실행**
   ```bash
   # 이유: 코드를 Nginx 이미지에 담아 빌드한 뒤, 백그라운드(-d)에서 상시 기동시키기 위함입니다.
   docker-compose up -d --build
   ```
2. **구동 확인**
   웹 브라우저를 열고 `http://서버IP:8080`으로 접속하여 정상 동작하는지 확인합니다. (기본 포트는 `8080`으로 설정되어 있습니다.)
3. **서버 중지**
   ```bash
   # 이유: 기동 중인 컨테이너 서비스를 안전하게 종료하고 리소스를 반환하기 위함입니다.
   docker-compose down
   ```
